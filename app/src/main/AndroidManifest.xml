<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dormitoryapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DormitoryApp">
        <activity android:name=".isSharalar"></activity>
        <activity android:name=".bron" />
        <activity android:name=".tarbieshiler" />
        <activity android:name=".upai" />
        <activity android:name=".kuntizbe" />
        <activity android:name=".onboarding3" />
        <activity android:name=".onboarding2" />
        <activity android:name=".onboarding1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>